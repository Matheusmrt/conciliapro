# Copie para .env e preencha antes de rodar docker compose up

# Banco de dados
POSTGRES_PASSWORD=senha123

# API
JWT_SECRET=mude-em-producao-use-openssl-rand-base64-32
SALT=mude-em-producao-tambem

# URL pública da API (usada pelo frontend no build)
API_URL=http://localhost:3001
